<div class="nav-scrollable" style="background-color: #0f172a; height: 100vh;">
    <nav class="nav flex-column pa-2">
        <div class="nav-item px-3 py-4">
            <MudText Typo="Typo.subtitle2" Style="color: #64748b; text-transform: uppercase; letter-spacing: 1px;">Menu Principal</MudText>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex align-center" href="" Match="NavLinkMatch.All" Style="border-radius: 8px; color: #cbd5e1;">
                <MudIcon Icon="@Icons.Material.Filled.Dashboard" Class="mr-3" Size="Size.Small"/> Home
            </NavLink>
        </div>

        <MudDivider Class="my-4 mx-3" Style="background-color: #1e293b;"/>
        
        <div class="nav-item px-3">
            <MudText Typo="Typo.caption" Style="color: #475569;">SISTEMA ARKHOS v1.0</MudText>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}